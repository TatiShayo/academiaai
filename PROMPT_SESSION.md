You are finishing AcademiaAI. Only 1 task remains!

═══ CURRENT STATE ═══
21 of 22 tasks done. 1 remaining.
PHASE 5: ADVANCED — 1 task left.

═══ LAST TASK ═══

Leaderboard / Social proof counter:
- Add to landing page (/page.tsx) hero section: "AcademiaAI users have humanized X million words this month"
- Query: SELECT COALESCE(SUM(total_words_processed), 0) FROM profiles
- Display formatted number (e.g., "2.4M words") with a subtle count-up animation
- Animated counter component: use framer-motion useSpring or a simple CSS counter animation
- Also display in dashboard sidebar or footer
- Make sure total_words_processed is updated in the humanize/enhance API routes when documents are processed

═══ RULES ═══
npm run build — must pass.
git add -A && git commit -m "done: leaderboard counter with word count"
Mark [x] in PLAN.md.

Then output: ✅ ACADEMIAAI COMPLETE — ALL 22 TASKS DONE

Start: Build the leaderboard counter.
